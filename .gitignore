# 本地备份与临时产物不入库（.mt-cache.json 翻译缓存要入库，跨天复用省配额）
.bak/
.tmp/
*.log

# 例句源数据缓存（44MB）；重新拉取命令见 tools/build-examples.mjs 顶部注释
tools/.examples-cache/
# 正文覆盖率对账（audit-coverage.mjs）的原文快照缓存。判定的是「库里 vs 当时的原文」，
# 缓存命中就不联网 —— 反复跑结果必须一致，所以缓存是正确性的一部分而不是提速手段。
# 体积大（每篇几十到几百 KB 的整页 HTML），不入库。
tools/.coverage-cache/
# 例外：两张小表是构建的必需输入，随仓库走
#   cet4-hf.json      17KB  真题高频表，build-core-vocab.mjs 用
#   cet4-sprint.json  36KB  冲刺池（真题表 ∪ CETVocabulary 词频），build-words-mid.mjs 用
#   cet4-gaps-general.json 1KB 通用高频向缺口词，build-core-vocab.mjs 用
!tools/.examples-cache/
tools/.examples-cache/*
!tools/.examples-cache/cet4-hf.json
!tools/.examples-cache/cet4-sprint.json
!tools/.examples-cache/cet4-gaps-general.json

# 本地密钥，绝不入库
tools/.deepl-key
tools/.dashscope-key
tools/.ecdict-blob.json

# 抓取残留 / 会话产物 —— 不参与构建、不该进公开仓库。
# 它们此前只是「未跟踪」，一旦有人走 git-status 路线 push 就会被一起传上去。
# _aesop-raw.html 另在 _api-push.mjs 的 NEVER_PUSH 里兜底，这里再拦一道。
.tmp-*.html
tools/.remote-articles.js
tools/_aesop-raw.html
# 本地工作区/会话目录（agent 记忆、计划稿），仓库公开，不入库
.zcode/
.workbuddy/
# .qoder/ 是 Qoder 生成的 repowiki 知识库（2026-09-21 实测 3.5MB / 185 个文件、无任何引用），
# 与 .zcode 同类。此前一直挂在 tree-diff 的 onlyLocal 里刷 185 行，把真正的漂移淹掉。
.qoder/
# .trae/ 同理（Trae 的本地工作区文档）。只忽略这两个 IDE 目录，不动 outputs/ 与根目录文档 ——
# 那些是内容与计划稿，归属别处，是否入库由人决定，不由这条规则替它决定。
.trae/

# 本地专用产物（个人偏好清单、私人备注）—— 统一后缀 *.local.*。
# 仓库是 public，GitHub Pages 还直接以 200 提供整个仓库根目录 → 推上去就等于公开。
# 两道保障：这里 gitignore 兜底，tools/_api-push.mjs 的 NEVER_PUSH_PATTERNS 硬拦截推送。
# 2026-09-17 事故：PEOPLE-ICONS-CHANGE-PLAN-2026-09-17.md（含 30 人偏好清单）经 --files 上了远端。
*.local.md
*.local.json

# 人工抽查材料（spot-check.mjs 生成，逐句对照 + 记录模板），属一次性产物
tools/_spot/

# 足球栏目（football.mjs）：raw/ 是原刊 HTML + 抓取缓存，只喂构建、不该进公开仓库；
# 栏目根下的 <id>.json 是**产出物**，要入库（与 raw/ 同目录是刻意的，靠这条只排除 raw/）。
# anchor-dict.json（ECDICT 释义词典，50KB）只服务于已废弃的锚点实验（见 lib-align.mjs 注释），
# 也一并排除 —— 真要复现实验用 .tmp/sweep-gloss.mjs 的思路重建即可。
tools/_football/raw/
tools/_football/anchor-dict.json

# 精翻通道（llm-refine.mjs）：_refine/ 是大模型精翻草稿 + 人工审校材料，本地专用不公开。
tools/_refine/

# ★ Android 壳的构建产物 —— 绝不入库（2026-09-22）。
#   mobile/www/ 是「assets/ + 抓取数据」拼出来的**第二份全文**；
#   outputs/apk/*.apk 更是把全部正文与封面打进一个 11MB 的包。
#   仓库是 public，GitHub Pages 还直接以 200 提供整棵工作树 —— 推上去就等于
#   把第三方版权内容重新分发一次（APK 审查报告第 ⑧ 条讨论的正是这件事）。
#   入库的只有「源码」：mobile/android/（清单 + res + 4 个 Java）、mobile/shell-glue.js、
#   mobile/version.json、tools/build-apk.mjs 等 —— 它们不含正文，可复现地构建出上面两个产物。
#   两道保障与 *.local.* 同：这里兜底，tools/_api-push.mjs 的 NEVER_PUSH_PATTERNS 硬拦。
mobile/www/
outputs/apk/
# audit 的端到端构建临时目录（[T] 节每跑一次建一个，名字带 pid+时间戳保证唯一）。
# 正常会被脚本自己在 finally 里删掉；加这条是防「删除失败留下的残骸」被 tree-diff
# 判成「本地有、远端没有」—— 那会诱导人把它推上去，而它其实是一份第二全文。
mobile/.audit-www*/
