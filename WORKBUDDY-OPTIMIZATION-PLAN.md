# WordLens 全文抓取与翻译优化：WorkBuddy 执行单

日期：2026-09-15。项目：`D:\四级词阅`。目标：读到完整、连贯、可核对原文的英文文章，中文译文忠实、自然。保留全文连续阅读。

## 0. 当前范围与交接

- 用户已要求删除明星栏目及全部明星文章，清空足球 / AI 文章。此次清理从 40 篇中移除 29 篇（明星 24、足球 2、AI 3），保留成长 11 篇。
- 明星入口已移除；足球 / AI 类别定义暂留，但空类别不会在首页和发现页显示；三类 RSS 已停用，`ingest --classics` 在联网前中止，daily 只采成长文章。不要擅自恢复这些类别。
- 保留文章的全部对象已在清理前后按 SHA-256 比较一致，包括英文、译文、段落、ID 和图片引用。此证据只证明清理没有更改保留文章，不能证明原先抓取完整。
- 清理后引用 53 张图片，移除文章留下的 410 张孤儿图片通过发布批次归档后从在线资源移除。清理前快照位于 `.bak/releases/20260915-140420/`，另有 `content-cleanup.json` 保存删除 ID、图片和保留数据摘要。此目录只留本地。
- 此次只做内容清理、停采、回归适配与交接计划。下面的全文与翻译优化尚待 WorkBuddy 实施。
- 用户授权本项目完成修改并验证后直接推送 `SHJDD134/ciyue` 的 `main`；不必另问一次。但不要把密钥、缓存快照或本地会话目录提交到公开仓库。

## 1. 已从代码确认的问题

| 位置 | 当前行为 | 实际影响 |
|---|---|---|
| `tools/ingest.mjs` 的 `MAX_SENTS` / `MAX_WORDS` / `packBlocks` | 非 full 来源默认截在 24 句 / 540 词 | 部分旧新闻是主动截短；当前成长 feed 有 full:true，但这只绕过预算截短 |
| `tools/lib-text.mjs` 的 `goodPara` | 小于 70 字符或 55 个英文字母的段落直接丢弃 | 简短转折、引语、问句和结尾可能消失 |
| 同文件 `splitSentences` | 过滤不超过 30 字符的句子；长句按逗号等拆块；把所有 `·` 还原为句号 | 句子可能被删、变成半句；原有中点也可能被误改 |
| `tools/ingest.mjs` 的 `extractBlocks` | 扫整个 HTML 的 p/figure/img；正文只按 p 提取；以前 60 字符去重 | 侧栏可混入，标题和列表可漏抓，前缀相同但内容不同的段落可能被删 |
| `tools/lib-mt.mjs` / `ingest.mjs` | 新版本给 DeepL 上下文，旧的有道 / MyMemory 降级路径仍无这份上下文 | 引入上下文不等于所有翻译引擎都具备语境，也不等于实际译文已改善 |
| `tools/lib-mt.mjs` 的缓存 | 已按方案名 + URL + 原句隔离，但不记录实际引擎、校验结论与全文版本 | 原文修订、供应商回显、被后续清洗拒收的译文可能持续命中旧缓存 |
| `tools/ingest.mjs` 的 `CN_POST` / `CN_POST_BY_CAT` | 用宽泛正则把“老板”替为“主帅”等 | 成长/商业正文也可能被错误改义；必须用原句与上下文约束 |
| `tools/lib-text.mjs` 的 `cleanPara` | 没有中文或拉丁字母占比高会被拒收 | 不能据此判定语义错误；公司名、产品名密集的正确译文可能误伤 |
| `qc.mjs` / 当前翻译测试 | 主要查结构、空译、回显、资源存在性；翻译请求用模拟服务验证 | 没有和独立的原文基准比对，无法证明无漏段、无漏句、无反义 |

以上是可复核的代码行为，不能据此声称已经查清每篇文章到底漏了多少。先对保留的 11 篇做证据核查。

本次清理验证：audit 182/182、导航 24/24、内容范围检查 6/6，冒烟检查缺失资源 0；QC 11 篇合格，但有 123 条警告（输出示例为 W2 短句），11 篇均未带新版评分字段。警告需要逐项分辨正常短句与错误断句，不应直接删除。普通 text-scan 通过仅说明未发现其规则覆盖的结构问题。

## 2. 第一阶段：建立原文对照基准（先做，暂不覆盖旧库）

1. 盘点当前 11 篇：ID、原文 URL、英文词数、段数、句数、首段和末段、图片数，保存清单。
2. 对每篇重新获取公开原页面，保存获取时间、响应状态、最终 URL、原始 HTML 和内容哈希。原始材料存 `.tmp/` 或 `.bak/`，不发布。
3. 人工查看正文容器，确定正文首尾、标题、列表、引用、图注。不能用当前 `goodPara` 的结果充当“完整原文基准”，否则过滤掉的内容永远无法被发现。
4. 输出逐篇差异：缺哪一段、漏哪一句、顺序是否变了、结束是否提前。遇到失效 URL、仅剩付费摘要、来源修改或正文无法确认，标为“未确认”，不计算为通过。
5. 产出 `tools/_spot/fulltext-audit/`：11 篇清单、原文/当前文对照页、差异报告、可重复使用的提取测试样本。重点覆盖最短与最长文章、短句多的访谈、图文交错文章。

验收：11 篇全部有明确结论和证据位置；不能只报“抽查通过”或虚构完整率。

## 3. 第二阶段：实现保留原文结构的提取链

1. 分离“发现链接”和“获取正文”：RSS 只用于找链接和元信息；摘要不能作为全文兜底。
2. 先定位各站点的正文容器，再依原顺序提取段落、h2/h3、li、blockquote、figure/figcaption；必要时为已选来源写专用适配器。取不到正文容器时留待检查，不能自动把整页文本当正文。
3. 清理导航、广告和订阅提示时，结合节点位置、类名和明确的文案特征。移除“短 = 垃圾”的硬规则，保留 `Why?`、`Not anymore.` 等正常短句。相同前 60 字符不能作为删段依据。
4. 保留完整原文，废除学习文章的句数/词数截断；篇幅太长可以不选，不许把截短内容标成全文。全文连续阅读，不增加阅读分页。
5. 断句仅用于中英对齐。使用稳定的段 ID / 句 ID；不要为 API 请求长度而改变文章的逻辑句子。请求分块与文章结构必须分开；临时缩写标记只能还原自己生成的部分。
6. 保留原文标题、列表、图注和图片位置。如需新增块类型，前端、词数统计、续读定位、清洗、发布校验一起兼容，不能只改抓取一端。
7. 给每篇加 `contentStatus: complete | partial | blocked | unverified`、提取版本和完整性证据。只有 complete 可以进待发布队列；检测到付费墙或只取到摘要时停止，不绕过访问限制，不补写正文。

验收：人工建立的正文基准，在只忽略空白/明确编码差异后，所有可见正文文本保留且顺序一致；首尾、短句、列表和引语均有断言。质量分高不能覆盖完整性失败。

## 4. 第三阶段：修复翻译链，建立真实质量样本

1. 先选 3 篇已确认完整的成长文章，包含抽象议论、故事叙述、访谈，形成 30—50 条人工核对样本，覆盖标题、代词、否定、数字、专名和结尾。保留修改前后译文作比较。
2. 以段落为上下文、句 ID 为映射单元。每篇发送标题、来源、相邻段落；单独翻标题和图注时也给对应正文语境。返回必须按 ID 验证：缺项、重复、多项、空白一律标记失败，不靠数组条数就判定成功。
3. 先验证当前 DeepL 的实际可用性与样本质量；只有样本表现不足时再评估替代服务。不要一开始就换模型，更不要从“主力引擎”注释推断当前实际使用了它。
4. 记录实际引擎、翻译时间、原文哈希、提取版本、翻译方案版本、状态与失败原因。缓存按全文版本/文本 ID/引擎方案隔离；失败或被验证拒收的译文不能以“成功缓存”无限重试。
5. 降级引擎的结果进入待复核队列，并明确记录降级；恢复主力服务后能选择只重译降级项。只重试失败段，设置超时、有限重试和退避；不为一个失败段重复翻整篇。
6. 移除不受语境约束的“老板→主帅”等正则；术语校对按栏目和英文原句限定。不要为了顺口增删事实，也不要用“有汉字”作为翻译质量充分条件。
7. 数量、日期、否定和主语一致性用于发现疑点，由对照检查确认；原文数字和中文汉字数字要归一后比较。仅人名/品牌的标题或图注应有专门规则，不能粗暴丢弃。

验收：对已核对样本，漏译、反义、数字错误、主语错配等严重错误为 0；报告样本数、发现与修复的具体问题、实际使用的引擎。模拟接口测试只证明代码流程，不能宣称实际翻译准确率。

## 5. 第四阶段：先修旧文，再恢复稳定供给

1. 在隔离的候选目录修复 11 篇旧文，保留文章 ID、原 URL 和可用图片，逐篇生成可读差异报告。先处理最明显缺段/结尾截断的文章，不整库一次性替换。
2. 无法确认来源的旧文标为待核对；不要擅自删掉剩余文章、换成新文章或编造补全文本。读者侧如需提示，用“正文完整性待核对 / 查看原文”，不用内部技术字段。
3. 修复旧文导致句子坐标变化时，迁移续读锚点（例如旧句文本哈希映射新位置）；找不到时给合理回退，不能悄悄跳到另一句。
4. 先稳定 2—3 个来源，当前的 6 个成长源逐个按全文可获取率、漏段情况、正文质量、标题匹配度评估。每天 1—2 篇合格内容是初始上限建议，不是强制产量；没合格文章就保留旧库。
5. 接入候选队列状态：discovered → fetched → extracted → translated → reviewed → published；失败原因可追查，允许断点重试和人工移出。不要让不合格的头两篇长期占着每日名额。

验收：每一篇上线内容有原文快照、提取结果、对照检查记录；已有阅读记录和生词本正常；失败运行不修改线上文章与配图。

## 6. 测试、发布和交付

- 新增离线提取样本测试：短句、同前缀不同段、列表/小标题、缩写与小数、付费摘要、空正文、图文顺序；测试基准必须独立于生产提取逻辑。
- 新增翻译失败测试：少一条、错 ID、重复 ID、原样回显、缓存拒收、429/超时、降级状态、原文修订后缓存失效；测试不消耗真实密钥或额度。
- 回归命令：`node tools/content-scope-test.mjs`、`node tools/qc.mjs --all`、`node tools/audit.js`、`node tools/nav-test.js`、`node tools/smoke.js`、`node tools/text-scan.js --full`，以及修改模块对应的专项测试。不要通过降低断言标准来消除失败。
- 触及发布/回滚模块才增加 `release-test.mjs`，运行前确认其当前版本确实保护现有工作树和发布基线。
- 本地 Git 索引显著落后远端：不要按 `git status` 的旧图片删除记录盲推，也不要 reset/clean 整个工作区。先核对远端 main，避免覆盖其他工具刚提交的更新。
- 发布使用 `node tools/publish.mjs --dry` → `node tools/publish.mjs` → `node tools/_api-push.mjs "说明本次具体改动" --manifest auto --files "显式列出所有改动和新增的代码文件"`。清单必须包括所有新模块/测试，不能重复遗漏 `mt-test.mjs` 一类新文件；备份、原文快照和密钥不得进入清单。
- 推送后核对远端文件 SHA、main、对应 Actions 成功和网站实际数据；本机读缓存成功不能当作线上已经更新。用户已授权修改验证后直接推送，结束时交付提交链接、网站链接、测试结果与尚未核实的限制。

## 可以直接粘贴给 WorkBuddy 的任务

请在 `D:\四级词阅` 阅读 `WORKBUDDY-OPTIMIZATION-PLAN.md`，按阶段执行全文抓取和翻译修复。先核对实际项目状态并建立现有 11 篇成长文章的原文对照，不恢复明星栏目或足球 / AI 文章。保留全文连续阅读、真实英文原文、稳定文章 ID、阅读进度与配图。先做完整性证据和提取链修复，再以真实样本改进翻译；不要只加字数门槛、换翻译引擎或跑结构 QC 就声称问题解决。各阶段都提交具体差异和测试证据，完成验证后按已有授权推送 main 并核验部署。不要修改无关词库、不要批量覆盖尚未核对的旧文。
