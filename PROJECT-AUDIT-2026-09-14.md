**词阅 WordLens 全项目检查报告 · 2026-09-14**

本次以当前工作区为准，检查了前端交互、本地数据、文章与图片、内容采集、质量门禁、缓存、发布和自动化流程。原始审计发现 11 项明确问题：4 项 P1、7 项 P2；均已在本次修复中处理。另列出现有功能缺口和需要继续验证的风险，避免把尚未实现的计划当成回归故障。

检查使用独立副本 `D:\四级词阅\.tmp\review-20260914`；故障注入、发布测试和浏览器缓存测试均在副本或独立浏览器会话内运行。未修改业务源码、用户真实阅读记录或线上站点。结束时核对 app.js、styles.css、文章数据、ingest、recommend、publish、qc、sw.js 和工作流，与检查副本一致。

**修复后复核（2026-09-14）**

本次已落地：导入结构校验；换文章、返回、切后台和关闭页面时保存阅读位置与百分比；阅读记录列表、搜索、未读完/已读完筛选和一键续读；首页分类入口跳转；Service Worker 当前缓存优先与首次在线资源预热；Actions 部署精确检出更新作业最终提交；Linux/Windows 图片压缩解释器选择与 Pillow CI 依赖；明星图片下载后的最终门槛、图数与图注词数复算；置顶专题不挤普通栏目配额；会员/联盟营销模板从足球正文移除；图片图注支持正文点词和可选中文图注。

复核结果：`qc --all` 合格 40/40，`audit.js` 182/182，`nav-test.js` 24/24，`sw-test.js` 9/9（含当前缓存优先），`smoke.js`、`text-scan.js`、`verify-ui.js` 均通过。真实 GitHub Actions、外站抓取和低端设备性能仍未在本地替代验证。

**证据与验证范围**

- 当前内容：40 篇文章（明星 24、成长 11、足球 2、AI 3）；463 张被引用的图片文件均存在，未发现缺图引用。
- 11 个现有检查脚本完成。带断言统计的测试共 299 条通过、0 失败；smoke、text-scan、verify-ui 也通过。46 个工具/应用 JavaScript 文件通过语法检查。
- push-test 使用本地模拟的 401 响应，未向真实远端发送写操作；其余数据发布与回滚测试在副本中执行。
- 使用真实 Edge 内核、390×844 视口补测点击导航、逐篇续读、错误备份、首次离线、旧缓存升级；这些新增检查发现了现有测试未覆盖的问题。
- 未触发真实 GitHub Actions 发布，未逐篇重新抓取外站或逐句人工审校全部译文。自动化两项问题来自代码路径和官方文档核对，未冒充云端实测结果。
- [现有测试结果](D:/四级词阅/.tmp/review-20260914/logs/results.json)、[浏览器复现结果](D:/四级词阅/.tmp/review-20260914/logs/browser-results.json)、[内容统计](D:/四级词阅/.tmp/review-20260914/logs/data-review.json)。

**1. [P1] 错误备份文件会覆盖、清空现有记录——已复现**

位置：[app.js](D:/四级词阅/assets/app.js:1605)。

`importData()` 只检查解析结果是不是对象，没有验证应用标识、版本和必需字段，随后直接替换状态并保存。实测先放入一条已读记录和一个生词，再导入内容为 `{}` 的文件，两者均变成空数组并写入 localStorage。这不是“导入失败不生效”，而是已经覆盖旧数据。

建议：先完整验证备份结构，再构造候选状态；确认可正常使用后一次提交。无关 JSON 应直接拒绝，并保留导入前状态用于失败回退。兼容旧版裸状态时，也必须要求可识别的阅读数据字段，不能接受任意对象。

**2. [P1] 点击“下一篇”会漏存上一篇阅读位置——已复现**

位置：[app.js](D:/四级词阅/assets/app.js:2007)，以及 [render 的离页保存条件](D:/四级词阅/assets/app.js:1848)。

`next-article` 只结算阅读时间，随即替换 activeArticle 和 lastRead。之后 render 仍处于 read 视图，不满足“离开阅读页”的位置保存条件。实测 A 篇滚到 2200px、约第 12 段，点击下一篇后 A 的 readPos 仍为空；回到列表重开 A，位置为 0。

建议：切换目标文章之前统一结算旧文章的位置、进度和时间。所有换文入口共用这一流程，不能依赖 render 的视图类型变化补存。

**3. [P1] 旧缓存可能长期遮住新版本——已复现**

位置：[sw.js](D:/四级词阅/sw.js:57)。

读取使用全局 `caches.match(req)`，而更新只写当前缓存；activate 又特意保留一个旧缓存。在旧缓存先创建、同一 URL 同时存在于新旧缓存的升级场景中，读取可能一直先命中旧内容。

真实浏览器复现：v42 保存过期的 OLD，v43 保存新鲜的 NEW；连续两次 fetch 同一资源，返回值均为 OLD。用不同查询版本号能够避开部分静态文件，但导航 URL、版本号未变的数据文件仍存在该问题。

建议：优先查询当前缓存，仅在未命中时回退旧缓存；将后台刷新和写缓存纳入 fetch 事件的 waitUntil 生命周期。补充多缓存测试，现有测试用一个 Map 模拟所有缓存，无法发现版本优先级错误。

**4. [P1] 定时更新后部署可能仍使用更新前的提交——代码与官方行为核对确认**

位置：[daily.yml](D:/四级词阅/.github/workflows/daily.yml:65)。

update 作业抓取并提交新数据，deploy 作业却重新执行不指定 ref 的 checkout。checkout 默认取触发这次工作流时的提交，而非前一作业刚推送的提交；通过默认 GITHUB_TOKEN 推送也不会额外触发一次 push 工作流来补上这次部署。因此，定时/手动流程可能显示成功，站点却落后于刚更新的数据。

建议：update 输出最终提交 SHA，deploy 精确检出该 SHA；或者将已校验的站点产物从 update 直接传给 deploy。不要只盲取 main 最新值，以免混入其他并发提交。

依据：[actions/checkout 默认 ref/SHA](https://github.com/actions/checkout#usage)、[GitHub 对 GITHUB_TOKEN 触发后续工作流的说明](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow)。本次没有触发云端发布。

**5. [P2] 首页分类按钮点击后仍停留在首页——已复现**

位置：[app.js](D:/四级词阅/assets/app.js:1957)，按钮定义在 [首页分类](D:/四级词阅/assets/app.js:937)。

首页分类按钮带 `data-go="1"`，事件处理器却只改变 catFilter 并重新渲染，没有切换到发现页。点击“明星”后，内部筛选值是“明星”，实际 view 仍为 home，用户看不到所选文章列表。

建议：区分首页入口和发现页内筛选，首页点击时进入对应分类并保存返回路径。

**6. [P2] 首次在线使用后，离线无法继续打开文章——已复现**

位置：[sw.js](D:/四级词阅/sw.js:92) 和 [Service Worker 注册](D:/四级词阅/assets/app.js:2232)。

Service Worker 在页面 load 后注册，首轮 HTML、脚本和数据请求已经结束；install 没有预缓存或补缓存。实测第一次在线打开、等待 worker ready 和控制页面后，Cache Storage 仍为空。立即离线刷新，仅出现“词阅 · 离线”提示页，无法访问刚看过的内容。

建议：完成安装时缓存最小应用壳和必需数据，或在控制权接管后主动补齐。离线可用状态应以缓存实际就绪为准，避免只注册 worker 就视为准备完成。

**7. [P2] 续读卡片百分比可能停留在旧值，甚至显示 0%——已复现**

位置：[app.js](D:/四级词阅/assets/app.js:1572)、[rememberReadPos](D:/四级词阅/assets/app.js:1494)。

正文 HUD 实时计算进度，但 lastRead.pct 只在五秒节流条件满足时更新。返回或切后台时，位置记录复制的还是旧 pct，没有重新计算当前百分比。实测正常返回前已滚到 1700px，保存记录 y=1700，pct 却为 0；从其他文章切回来时，旧百分比也可能被重置。

建议：实时更新内存位置和百分比，只对磁盘写入节流；退出、后台、换文时强制刷新并保存同一份记录。不要让首页卡片与正文 HUD 使用不同的新鲜度口径。

**8. [P2] Linux 自动更新无法执行当前图片压缩命令——代码路径确认**

位置：[ingest.mjs](D:/四级词阅/tools/ingest.mjs:434)、[daily.yml](D:/四级词阅/.github/workflows/daily.yml:35)。

Python 默认路径写死为本机 Windows 的 `C:/Users/sekiro/.workbuddy/.../python.exe`，工作流却运行在 ubuntu-latest，也未配置 WORDLENS_PY。压缩调用失败后只打印消息并继续。结果是自动新增图片不按预期缩小；较大新闻封面还可能被随后 250KB 的质检门槛拒收。

建议：按运行环境选择 Python，工作流明确安装 Pillow、传入解释器路径；对“压缩没有运行”给出明确的失败或可审查降级结果。

**9. [P2] 明星图片硬门槛没有覆盖下载后的最终内容——已复现门禁缺口**

位置：[ingest.mjs](D:/四级词阅/tools/ingest.mjs:1081)、[photoCount 与评分写入](D:/四级词阅/tools/ingest.mjs:1104)、[qc.mjs](D:/四级词阅/tools/qc.mjs:93)。

图片下载失败会跳过对应图片，但 photoCount、图片加分及图注词数仍按下载前的数据计算。后续 qc 对明星没有最终图片数量检查。

现有库已有 Martha Stewart 专题 `photoCount=16`、实际正文图 15 张的差异。在副本中移除一篇合格明星专题的全部 16 张正文图片，保留封面和文字，qc 仍返回退出码 0、合格 1 篇。由此可见，“至少六张图”的门槛可以在下载之后失效。

建议：从最终 paras 重新计算图数、图注词数与质量分；写入和发布前复查实际有效图片。图片下载失败导致低于门槛时，拒绝该篇并清理本次孤儿产物。

证据：[零正文图仍通过质检](D:/四级词阅/.tmp/review-20260914/logs/gallery-gate.json)。

**10. [P2] 置顶专题实际占用了普通文章配额——已复现**

位置：[publish.mjs](D:/四级词阅/tools/publish.mjs:100)。

代码注释和经典专题采集说明均承诺“置顶不占配额”，实际却用 `cap - pinnedIn.length` 计算普通文章名额。当前明星栏目 24 篇全部置顶，默认上限 25，因此只剩一个普通文章名额；以后置顶达到 25 篇，普通明星新文会全部被淘汰。

副本模拟加入两篇当日普通明星文章后，dry-run 从 42 篇裁到 41 篇，淘汰其中一篇。建议置顶与普通文章配额分别计算，并补上“24 篇置顶＋两篇新文”的测试。现有测试仅证明置顶自身不被删，没有验证它不挤占其他文章。

证据：[配额复现](D:/四级词阅/.tmp/review-20260914/logs/pin-quota.log)。

**11. [P2] 两篇足球文章混入网站推广文字，清洗检查没有拦住——实际数据确认**

位置：[文章数据](D:/四级词阅/assets/data-articles-extra.js:11170)、[另一处推广声明](D:/四级词阅/assets/data-articles-extra.js:11190)。

当前两篇足球文章都包含会员权益推广和通过链接购买产生佣金的声明，而且被翻译成中文、作为正文显示并参与词汇与难度统计。真实阅读截图也可看到这些段落。这会打断文章，并让读者学习与报道无关的内容。

建议：按来源限定正文容器，增加已确认的会员推广、联盟营销声明过滤规则；同步清理已入库内容。不要把所有 newsletter、会员或网站相关词一律删除，以免误删访谈原话。此次广泛关键词扫描也产生了此类误报，报告只把两篇足球文章中的明确模板文字列为缺陷。

证据：[阅读截图](D:/四级词阅/.tmp/review-20260914/logs/mobile-reading.png)。

**功能缺口与性能风险，单独列示**

- 阅读记录页面、关键词/状态筛选、每篇进度和已下线文章的记录快照已实现；已下线文章只能查看记录，不能继续打开正文，这是数据不在本地存档时的客观限制。
- 明星历史通道已实现且当前有 24 篇专题；daily.mjs 现在每天以尽力方式调用 `ingest.mjs --classics`，网络或站点改版失败时保留旧内容并继续 RSS 更新。
- 图注现在支持正文点词；新抓取的历史专题会保存可选中文图注。图注仍未纳入逐句朗读与独立进度统计，避免把图注当作普通段落改变原有阅读节奏。
- 初始数据脚本合计约 5.12MB，未包含 app.js、样式和图片，是未压缩文件体积，不是实际传输量。随着永久保留的专题增多，全库首屏加载会持续增长。尚未在低端手机与弱网下测得实际卡顿，建议后续测量后再决定按需加载方案。
- 当前自动检查多数使用 Node VM 和简化 DOM；没有真实浏览器的端到端门禁。push 部署也跳过 update 作业的测试。此次全部现有测试通过仍发现多项实际故障，说明应补充关键用户路径测试，而非只增加渲染字符串断言。
- 未重新核验所有外站来源可达性、文章完整性及每句译文语义；本次“没有乱码/漏译结构错误”不等同于“译文全部准确”。

**建议修复顺序**

1. 先处理备份覆盖、换文漏存、旧缓存优先级和部署提交错误，分别保护用户记录与版本一致性。
2. 再修首页分类、进度百分比和首次离线，完成日常阅读闭环。
3. 修正压缩运行环境、最终图片门槛、置顶配额和正文清洗。
4. 在上述基础上实现完整阅读记录、历史专题定时补充及图注交互。

修复验证应覆盖本报告的实际复现步骤。真实部署、远端抓取和低端手机性能仍需在对应环境单独验证。

