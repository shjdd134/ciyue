/* 词阅 WordLens —— 封面图映射（自动生成，请勿手改；运行 node tools/ingest.mjs --backfill 重新生成）
 *
 * 键为文章 id，值为本地图片路径（相对站点根目录）。
 * 抓不到配图的文章不在此表内，页面会自动回退到渐变封面。
 * 共 0 篇有图（2026-09-12 内置种子文章清理后暂无映射；抓取文章封面自带 coverImg）。
 */

const COVER_MAP = {};
