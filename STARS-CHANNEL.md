> 第二阶段方案（2026-09-14）。前序验证见 `STARS-SAMPLE.md`（三人样本）；本文回答「英文到底有没有货」
> 并给出**历史通道**的落地方案。所有数字为实抓真值，核查脚本在 `tools/_spot/`（已 gitignore）：
> `src-probe.py`（货源可达性）、`gallery-probe.py`（图集实测）、`classic-probe.py`（经典回顾精扫）、
> `extract-check.py`（提取规则）、`vogue-slides.py`（分类占比）。

# 明星栏目「历史通道」——货源实证与落地方案

## 决策（2026-09-14 用户已定）

| 项 | 决定 | 影响 |
|---|---|---|
| 图片托管 | **A 下载展示**（与现站一致，图存本地） | 走现有 `img-post.py` 链路，不改架构；版权风险自负 |
| 人物范围 | **只收女性** | 靠 slug 人物名单 + 图注人物识别双重过滤 |
| 图片尺寸 | **维持 720px**（全站统一） | `img-post.py` 不动；放大看细节的效果有限，先接受 |
| 历史通道 | **开** | 新增 sitemap 非 RSS 采集路径（本节 §3） |
| `pin` 长期保留 | 先不管 | 机制已存在，要用随时打标 |
| 内容范围 | **单人女性 + 旧照回顾**（见下） | 主题型群像不收；「历代妆容盘点」类不收 |

> 阶段一（内容样本 + 名单定稿）**已完成**，产出见根目录 **`STARS-CANDIDATES.md`**，
> 中文对照 **`STARS-CANDIDATES-ZH.md`**，由 `node tools/fetch-classics.mjs --months 8 --md STARS-CANDIDATES.md` 生成。
> 结论：近 8 个月扫出 1292 条图集 URL → 60 条经典回顾型 → **定稿入选池 24 条 / 24 人**
> （单人女性 + 早期影像 ≥10 张，中位数 22 图）。**「货」的问题已经解决，可以进阶段二。**
>
> 用户对初筛清单的复核结论（2026-09-14）：删掉① 图多但早期影像 0—8 张的「历代妆容盘点」11 条、
> ② 主题型群像 15 条、③ 同人物重复篇 2 条。
>
> **⚠️ 文字门槛后来改过，本文档下方若写「正文 ≥300 词」以本节为准**：
> 单看正文词数是**量错了对象** —— 那是照新闻文章标的线，而 Vogue 经典回顾的本体是图集，
> 正文中位数只有 185 词、图注中位数 362 词，把「这张旧照是谁、哪年、穿的什么」讲清楚的正是图注。
> 按「正文 ≥300 词」卡，29 条满足全部内容规则的候选里只剩 **3 条**（碧昂丝 45 图 / 41 张早期影像，
> 正文 178 词 + 图注 641 词，被判「文字太薄」）。用户当日拍板改为
> **正文 ≥80 词 且 正文+图注 ≥300 词**，池子回到 **24 人 / 26 条**（每人一篇后 24 条）。
> 代价要记住：**图注在阅读页渲染成 `<figcaption>`，不进句子流 —— 不可点读、没有中文译文**，
> 所以一篇的「中英对照」只有那 80—250 词的导语。让图注也变成可点读的正文属第三阶段展示层改造。

## 0. 三个结论

**① 英文有货，而且充足。** Vogue 美国版 `/slideshow/` 与英国版 `/gallery/` 是专门的图集栏目，
近 8 个月命中「经典人物影像回顾」**47 条**（US 30 + UK 17），全是对味的内容：
`vintage-pictures-of-charlotte-rampling`、`charlize-theron-in-the-1990s`、`sophia-loren-european-summer-muse`、
`audrey-hepburn-givenchy-film-looks`、`most-iconic-photographs-of-princess-diana`、
`venice-film-festival-throwback-photos-from-the-90s-and-early-aughts`。

**② 历史通道可以零人工维护。** 我原本以为「按人物找档案」必须手工维护 URL 清单——
**实测不需要**：Vogue 的 sitemap 按月分片（`sitemap-2026-09.xml`，共 60 片 ≈ 5 年），
扫描一次即得全量图集 URL，新内容每月自动出现。**「人工维护」这个成本在方案里可以划掉。**

**③ 形态与现有数据模型天然吻合。** 这些专题不是「一张图配一句话」的 slideshow，
而是 **783—2229 词的完整导语 + 16—54 张图 + 每张带拍摄年代的图注**。文本量足够做点词查义与逐句对照。

---

## 1. 货源清单（实测）

「RSS 内图」= feed 的 description 里 `<img>` 的数量；0 表示要抓原文（现有管线 `full: true` 已支持）。

### A 类 · 经典回顾型专题（主力货源）

| 来源 | 栏目入口 | 发现方式 | 近 8 月产量 | 语言 | 备注 |
|---|---|---|---|---|---|
| **Vogue US** | `/slideshow/` | sitemap 月度分片 | 30 条 | 英文 | 8 个月共 857 条 slideshow，其中经典回顾 30 条 |
| **British Vogue** | `/gallery/` | sitemap 月度分片 | 17 条 | 英文 | 8 个月共 435 条 gallery |

> **必须说清楚的占比真相**：857 条 slideshow 里 54% 是**品牌派对／开业／时装周后台**（`mytheresa-party`、
> `alo-nyfw-party`…），图注只有人名、没有文本，**不是你要的内容**。真正对味的「经典回顾」是长尾里那 30 条。
> 所以历史通道的筛选器必须**主动排除商业活动**，不能靠「Vogue 的图集都行」。

### B 类 · 人物生平影像

| 来源 | 系列 | 实测 | 语言 |
|---|---|---|---|
| **BBC News** | `In pictures` / "A life in pictures" | 38 图 / 20 图注 / **7 个 ≤2005 年代** | 英文 |

BBC 的 "Dolly Parton: A life in pictures" 是标准的「人物成长影像」形态，
图注如 `Dolly Parton, aged five`、`Dolly Parton and family at Christmas in 1960`。

### C 类 · 复古影像站（RSS 自带多图，抓取最省事）

| 来源 | RSS 内图中位 | 条目 | 语言 | 说明 |
|---|---|---|---|---|
| **Messy Nessy Chic** | **38 张/条** | 16 | 英文 | 复古／文化，图直接在 RSS 里 |
| **Flashbak** | **16 张/条** | 12 | 英文 | 复古影像档案 |

这两家是 20 多个候选里**唯二「RSS 本身带图集」**的，加进 `FEEDS` 即可，不用写抓取器。

### D 类 · 现有源补位（抓正文即可）

Vanity Fair（30 条/次）、ELLE（50）、Harper's Bazaar（50）、Town & Country（50）、Tatler（30）、
Guardian `inpictures`（22）、Guardian `art&design`（23）——RSS 内图均为 0，靠 `full: true` 抓正文。

**产量核算**：A 类 47 条 ÷ 8 月 ≈ **每月 6 条**；加上 B/C/D 补位，**每月 8—16 条可达** ——
正好覆盖你定的「每周争取 2—4 篇」。

---

## 2. 形态实测（入库可行性）

提取口径：**限定 `<article>` 容器内的 `<figure>`**，图注取 `<figcaption>`。

| 专题 | figure | 图注 | 图注带年代 | ≤2005 年代 | 正文词数 |
|---|---|---|---|---|---|
| 戴安娜王妃时尚回顾（BV） | **54** | **53** | 53 | **16** | **2229** |
| Beyoncé 45 张成长回顾 | 46 | 45 | 45 | 9 | 1035 |
| 威尼斯影展 90 年代回顾 | 41 | 39 | 39 | 7 | 1161 |
| 戴安娜王妃影像（BV） | 26 | 25 | 17 | 13 | 856 |
| Hugh Grant 早年影像 | 16 | 15 | 15 | 12 | 783 |

**每一行都超过「≥6 张有效图」的门槛，接近或超出「理想 8—16 张」；图注几乎 100% 带拍摄年份；
正文词数 783—2229，做英语学习绰绰有余。**

图注原文样例（可直接落库为 `cap`）：

```
Photo: Getty Images  November 1980  A 19-year-old Diana Spencer wore a red blazer,
pinstripe skirt and black sweater on a London street.
Photo: Getty Images  1990  A nine-year-old Beyoncé and four-year-old Solange
celebrating Christmas at home.
```

→ **你要的「保留原图顺序、图注、年代与出处」在数据上完全成立**，出处（Getty Images）与年代都在图注里。

**图片实测**：可直接下载（HTTP 200 / image/jpeg），**原始尺寸 2580px 宽**（`w_2580` 参数可达），
单张约 324 KB。当前站内统一压到 720px，这里**有清晰度余量**。

---

## 3. 历史通道技术方案

### 3.1 发现层 —— sitemap 扫描，零人工

```
sitemap.xml（索引）→ sitemap-YYYY-MM.xml（月度分片）→ 全量 URL
  → 筛 /slideshow/ 或 /gallery/
  → slug 关键词分类（见下）
```

筛选器（实抓验证过，`classic-probe.py`）：

- **保留**（经典人物影像）：`style-evolution` / `through-the-years` / `best-moments` / `most-iconic` /
  `vintage-pictures` / `throwback` / `of-all-time` / `retrospective` / `archive` / `(?:19|20)\d0s` / `aughts`
- **排除**（商业活动／非人物）：`party` / `opening` / `launch` / `dinner` / `celebration` / `anniversary` /
  `birthday` / `backstage` / `front-row` / `diary` / `street-style` / `collection` / `runway` /
  `wedding` / `home` / `travel` / `shopping`

这套正则在 857 条上跑出的就是第 1 节那份 30 条清单，误报/漏报已人工过目。

### 3.2 抓取层 —— 以 `<figure>` 为准，**不是**数 `<img>` 标签

⚠️ **这条必须写进代码注释**：Vogue slideshow 页面 `<img>` 标签只有 7 个，
而 `<figure>` 有 16 个（它用 `<picture>` + `<source srcset>`）。
**按 `<img>` 标签计数会低估一半以上。** 正确口径：

```
article = <article>…</article>（回落 <main>）
for each <figure> in article:
    img  = figcaption 同级 <picture>/<img> 的最大 srcset 候选
    cap  = <figcaption> 纯文本（清洗 "Native Share" / "1/53" 噪声）
```

### 3.3 入库层

复用现有 `paras[{type:"img", img, cap}]` 模型（`assets/data-articles-extra.js`），新增：

- `pin: true` —— 经典专题长期保留（机制已存在，`publish.mjs:77,87`，全库尚未用过）
- `src: "vogue-uk"` / `yearFrom: 1980` / `license: "getty"` —— 收录方式与年代，供筛选与合规展示

### 3.4 图片层

- 现在：全站 `img-post.py` 统一 720px / q82
- 明星栏目：建议单独放宽（实测原图 2580px，放宽到 1080—1400px 肉眼差别就很大）

---

## 4. 必须一起修的三个技术缺口

| # | 问题 | 现状 | 位置 |
|---|---|---|---|
| 1 | **`cap` 字段没进库** | 数据模型有 `{img, cap}`，实测 **59 张图 `cap` 全空** | `ingest.mjs:501` 有读 `<figcaption>` 逻辑但真实链路未生效 |
| 2 | **图注噪声** | Vogue 图注含 `Native Share`、`1/53`、`Photo: Getty Images` 前缀 | 需清洗规则 |
| 3 | **图片托管与版权** | 见 §5 | 新问题，无现成机制 |

---

## 5. 版权 —— 必须你拍板

这些图是 **Getty Images 版权**（Vogue 授权使用）。站内现有 78 张封面／内嵌图是**下载到本地 `assets/`** 的做法，
直接套用到明星栏目，风险等级不同（封面是编辑用途的原站图，这里是图库版权图）。三种方式：

| 方式 | 做法 | 代价 |
|---|---|---|
| **A 下载展示** | 与现站一致，图存本地 | 版权风险最高，但用户体验最好（可离线、可缩放） |
| **B 缩略图 + 跳原文** | 只存低清缩略图做卡片，详情页外链 Vogue 原文 | 合规稳妥，但破坏阅读体验（读一半跳走） |
| **C 外链热链** | 直接引用 `media.vogue.co.uk` 的 URL | 版权风险同在，且对方改 URL 就挂 |

你原方案里写过「来源的收录方式也应单独记录，支持按允许方式展示或跳转原文」——
**这个「收录方式」字段就落在 §3.3 的 `license` 上**，三种方式可以混用（不同源用不同策略）。

---

## 6. 三项决策 —— 已定（见文首「决策」表）

1. **图片托管** → A 下载展示（与现站一致）
2. **人物范围** → 只收女性
3. **图片尺寸** → 维持 720px

（`pin` 长期保留先不管；机制已存在（`publish.mjs:77,87`），要用随时打标即可。）

---

## 7. 实施计划

### 第一步 · 最小闭环 —— ✅ 已完成（2026-09-14）

`tools/fetch-classics.mjs`：sitemap 扫描 → 关键词筛选 → 人物识别 → 抓取实测 → 产出候选清单。
**没有写库、没有发线上。** 产物是根目录 `STARS-CANDIDATES.md`（人看）+ `tools/_spot/classics-candidates.json`（机器读）。

实测（近 8 个月）：

| 阶段 | 数量 |
|---|---|
| 全量图集 URL | 1292 |
| 命中「经典回顾型」 | 60 |
| 其中 slug 确认女性人物 | 43 |
| 实测成功 | 58 |
| 跨站重复（已归并） | 3 |
| 初筛主力候选（女性人物 + ≥6 图） | 39 |
| **用户过目后定稿的入选池** | **25** |

### 第一步补 · 名单定稿（2026-09-14）

用户看过初筛清单（含中文对照）后定了四条硬规则，已写进 `fetch-classics.mjs` 的 `buildPool()`：

1. **名单确认女性** —— 主题型群像不收（威尼斯影展／Met Gala／模特封面那 15 条）。
2. **图注带 ≤2005 年份的图 ≥10 张** —— 这条是关键，见下。
3. **同一人物只留一篇**（取早期影像多的那版）。
4. **图 ≥6 张 + 正文 ≥80 词 且 正文+图注 ≥300 词**（2026-09-14 校准，原为单看正文 ≥300 词，见文首说明）。

**为什么必须加第 2 条**：初筛里图数最多的几条恰恰不是想要的。
爱莉安娜（37 图）、赞达亚（31 图）、泰勒·斯威夫特（26 图）全是「历代妆容盘点」，
图注年份集中在近几年，早期影像 0—8 张 —— 按「拍摄年代」判断根本不算旧照回顾。
**「图片多」不能当入选条件**，这是实证。

排除明细（30 条）：

| 类别 | 条数 | 说明 |
|---|---|---|
| 同人物重复 | 2 | 戴安娜、维多利亚·贝克汉姆各多一篇 |
| 文字太薄 | 3 | 正文 <80 词 或 正文+图注 <300 词 |
| 早期影像 <10 张 | 11 | 「历代妆容／造型盘点」类 |
| 主题型群像 | 15 | 按用户决定排除 |

入选池里排前列的（括号内为「≤2005 年图注张数 / 正文词数 + 图注词数」）：
戴安娜王妃（53 / 482+1692）、碧昂丝（41 / 178+641）、芭芭拉·史翠珊（29 / 92+1019）、
维多利亚·贝克汉姆（28 / 238+373）、安妮公主（24 / 140+365）、哈莉·贝瑞（22 / 108+469）、
梅丽尔·斯特里普（22 / 148+412）、夏洛特·兰普林（21 / 322+278）、
萨尔玛·海耶克、娜塔莉·波特曼、莎拉·杰西卡·帕克、佩内洛普·克鲁兹、麦当娜、凯特·布兰切特…
（还有伊丽莎白·泰勒、奥黛丽·赫本、杨紫琼、蕾妮·齐薇格、查理兹·塞隆等）

> 早期版本这里写的是「戴安娜 2215 词、碧昂丝 1035 词」——那是**错的**。
> 那个数字把 Vogue 每个 slide 外面重复的那份图注 `<p>`（`Expand Photo: Getty Images 5/53 …`）
> 也算进了正文。真正文词数见上表。这个口径错误一度还导致「清单说能入、入库被拒」的漂移，
> 修法与守卫见 `lib-classics.mjs` 的「正文判定与词数」一节。

**24 条远超「每周 2—4 篇」的需求——货不是问题，接下来是接入管线。**

中文对照见根目录 `STARS-CANDIDATES-ZH.md`（含每条「是谁」与排除原因）。

跑出来的四个坑（都已写进代码注释，别再踩）：

1. **不能数 `<img>` 标签**，要数正文容器内的 `<figure>`。
2. **老版模板的图片 URL 没有扩展名**（`…/w_320,c_limit/542247402`），
   用扩展名白名单过滤会把整篇图集滤成「0 张图」——看起来像「这篇图少」，
   实际是解析器不认识它。实测 `vintage-pictures-of-charlotte-rampling`（22 个 figure）就是这么变成 0 的。
3. **不能只取第一个 `srcset`**：手机断点的 source 排在前面，宽度只有 320w。
4. **跨站去重不能靠图注文本或图片 id** —— Vogue 美英两版各写各的图注、各传一份图片副本
   （photo id 交集为 0）。可用的是「同一人物 + 图数接近 + 年代集合重合」。

### 第二步 · 接入管线（**已完成并上线，2026-09-14 晚**）

- `tools/ingest.mjs --classics`：sitemap 历史通道、`cap` 进库、明星图上限 4→16、`pin:true` 入库。全量跑完 **24 篇经典专题入库**（戴安娜、碧昂丝、芭芭拉·史翠珊、奥黛丽·赫本、安妮公主、杨紫琼等 24 人，每人一篇）。
- `tools/recommend.mjs`：明星栏目图片硬门槛 `meetsImageGate`（≥6 图）；`qualityScore` 新增 `capWords`（图注词数）——图集的正文薄是常态，按新闻文章的尺子打分会把 92 词正文 + 1000 词图注的芭芭拉·史翠珊拒之门外。
- 入选文字门槛（用户定稿）：**正文 ≥80 词 且 正文+图注 ≥300 词**（图注在阅读页渲染为 `<figcaption>`，是真实可读内容）。
- 删除旧 3 篇明星娱乐短讯（2/3/3 图、封面断链、不属女性专题）。库现为 40 篇：16 RSS + 24 经典专题。
- 发布：批次 20260914-214239（460 项）+ 3 个补丁 commit，全部上线。发布前发现并修复 **published.json 被 release-test 污染**的事故（见 `tools/repair-published.mjs` 头注释）。
- 回归：audit 182/0 · nav 18/0 · qc-test 5/5 · recommend-test 16/0 · release-test 26/26 · sw 8/8 · examples 28/28 · push 9/9 · qc 拒收 0。

### 第三步 · 展示层（你原方案的「最后优化展示」）

大图卡片、人物筛选、全屏相册、清晰图按需加载。图注目前是纯英文 `<figcaption>`；让图注也进句子流（可点读、有译文）属这一步，届时文字门槛可放宽到「正文 ≥80」。

> **第一阶段的验收标准**（你定的）：打开明星栏目，连续几篇都是想看的人、有足够好看的相关照片、也有能顺着读下去的文字。
> 39 条主力候选已经证明「货」够（图 22 张中位数、正文 755 词中位数、图注带确切年代与出处）；
> 第二步接入管线、第三步做完展示，这个验收才算真正过关。

